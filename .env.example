# Nova AI provider keys (all optional — the app runs offline with the
# built-in tutor + pollinations even when none are set). Add the ones you
# want on the deploy host (Render/Railway dashboard -> Environment).
#
# GEMINI_API_KEY=
# GROQ_API_KEY=
# OPENROUTER_API_KEY=
